{
  "name": "leave-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack web application for managing employee leave.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@lms/web",
    "build:web": "npm run build --workspace=@lms/web",
    "lint:web": "npm run lint --workspace=@lms/web"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}